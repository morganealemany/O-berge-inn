<form method="post" action="{{ path('backoffice_event_delete', {'id': event.id}) }}" onsubmit="return confirm('Souhaitez-vous vraiment supprimer cet évènement?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ event.id) }}">
    <button class="btn button_connexion">Supprimer</button>
</form>