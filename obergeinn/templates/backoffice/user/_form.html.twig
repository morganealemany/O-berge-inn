{{ form_start(form) }}
    {{ form_widget(form) }}
    <div class="admin-user-edit-button">
        <button class="btn btn-dark">{{ button_label|default('Save') }}</button>
    </div>
{{ form_end(form) }}
